<div class="container">
  <div class="row">
    <div class="col-sm-6 col-sm-offset-3">
    
      <h1><%=form_title%></h1>
      
      <%= form_with(model: blog, local: true) do |form| %>
        <% if blog.errors.any? %>
          <div class="alert alert-danger" id="error_explanation">
            <ul><%= pluralize(blog.errors.count, "error") %> prohibited this blog from being saved:</ul>
            <ul>
            <% blog.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
            </ul>
          </div>
        <% end %>
          
          <div class="form-group">
            
            <%= form.text_field :title, id: :blog_title, class: "form-control input-lg", placeholder: "Input title" %>
          </div>

          <div class="form-group">
            <%= form.text_area :body, id: :blog_body, class: "form-control input-lg", placeholder: "Input body"  %>
          </div>

          <div>
            <%= form.submit form_title, class: "actions btn btn-prymary btn-lg btn-block" %>
          </div>
          <div>
            <%= link_to 'Back', blogs_path, class: "actions btn btn-prymary  btn-block"  %>
          </div>

        <% end %>


    </div>
  </div>
</div>